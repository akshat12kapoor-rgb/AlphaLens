# Combined AlphaLens app (app.py) - union of the surface requirements.
# Floors below are what the combined app was verified against; the shell uses
# st.navigation, container(height="stretch") and width="stretch".
streamlit>=1.63.0
yfinance>=0.2.40
pandas>=2.0.0
numpy>=1.26.0
plotly>=5.18.0
ta>=0.11.0
